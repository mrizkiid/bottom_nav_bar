import 'package:flutter/material.dart';

class MessengerPages extends StatelessWidget {
  const MessengerPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: const Center(
        child: Text('Message Page'),
      ),
    );
  }
}
